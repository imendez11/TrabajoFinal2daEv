package com.example.peliculas_api;

import android.os.Bundle;

import androidx.appcompat.app.AppCompatActivity;

import com.example.peliculas_api.view.ConfirmarContract;

public class ConfirmarCompra extends AppCompatActivity implements ConfirmarContract {

    protected void onCreate(Bundle savedInstanceState) {

        super.onCreate(savedInstanceState);
        setContentView(R.layout.confirmar_compra);


    }
}
